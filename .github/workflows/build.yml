name: Pipeline de Calidad y Seguridad (Sonar, Semgrep, Snyk)
on:
  push:
    branches:
      - main
      - master
  pull_request:
    types: [opened, synchronize, reopened]

jobs:
  sonarcloud:
    name: SonarCloud
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v3
        with:
          fetch-depth: 0  # Importante para que Sonar pueda comparar versiones

      # 1. Instalar Java 17 (Requerido por el escáner de Sonar)
      - name: Set up JDK 17
        uses: actions/setup-java@v3
        with:
          java-version: 17
          distribution: 'zulu'

      # 2. Instalar Flutter (TU VERSIÓN EXACTA)
      - name: Set up Flutter
        uses: subosito/flutter-action@v2
        with:
          channel: 'stable'
          flutter-version: '3.35.2' # ✅ Actualizado a tu versión local

      # 3. Descargar librerías del proyecto
      - name: Install dependencies
        run: flutter pub get

      # 4. Ejecutar TODOS tus tests y generar el archivo lcov.info
      - name: Create Dummy Service Account
        run: |
          mkdir -p assets/keys
          echo '{}' > assets/keys/service_account.json

      # Ejecutamos tests y generamos lcov.info
      - name: Run Tests with Coverage
        run: flutter test --coverage --dart-define=API_TOKEN="${{ secrets.API_TOKEN }}"

      # Corregir rutas para que Sonar encuentre el lcov.info
      - name: Fix Coverage Paths
        run: |
          # Reemplazar "SF:lib/" por la ruta completa del servidor
          sed -i "s|SF:lib/|SF:$GITHUB_WORKSPACE/lib/|g" coverage/lcov.info
          
          # (Opcional) Imprimir las primeras lineas para verificar en el log si funcionó
          echo "--- Contenido del lcov.info (primeras 5 lineas) ---"
          head -n 5 coverage/lcov.info

      # 5. Enviar el reporte a SonarCloud
      - name: SonarCloud Scan
        uses: SonarSource/sonarqube-scan-action@v6.0.0 # 👈 Actualizado a v6
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
          SONAR_TOKEN: ${{ secrets.SONAR_TOKEN }}
        with:
          args: >
            "-Dsonar.organization=cdhm"
            "-Dsonar.projectKey=dennisdhm7_proyecto_sosmascota"
            "-Dsonar.sources=lib"
            "-Dsonar.tests=test"
            "-Dsonar.dart.lcov.reportPaths=coverage/lcov.info"
            "-Dsonar.coverage.reportPaths=coverage/lcov.info"
            "-Dsonar.exclusions=test/**/*,lib/**/*.g.dart,lib/firebase_options.dart"

  # -----------------------------------------------------------------
  # JOB 2: SEMGREP (Análisis Estático de Seguridad - SAST)
  # Busca patrones de código inseguros (hardcoded secrets, malas prácticas)
  # -----------------------------------------------------------------
  security-semgrep:
    name: Security - Semgrep
    runs-on: ubuntu-latest
    # No necesita Flutter instalado, solo el código fuente
    container:
      image: returntocorp/semgrep
    if: (github.actor != 'dependabot[bot]') # Evitar correr en PRs automáticos
    steps:
      - uses: actions/checkout@v3
      
      - name: Run Semgrep
        run: semgrep scan --config=p/default --config=p/security-audit --config=p/secrets

  # -----------------------------------------------------------------
  # JOB 3: SNYK (Análisis de Dependencias - SCA)
  # Busca vulnerabilidades en los paquetes de pubspec.yaml/lock
  # -----------------------------------------------------------------
  security-snyk:
    name: Security - Snyk
    runs-on: ubuntu-latest
    # Eliminamos 'needs' para que corra en paralelo
    steps:
      - uses: actions/checkout@v3
      
      # 1. Instalamos Flutter (Necesario para generar el lockfile correcto)
      - name: Set up Flutter for Snyk
        uses: subosito/flutter-action@v2
        with:
          channel: 'stable'
          flutter-version: '3.35.2'
          
      # 2. Generamos el pubspec.lock (VITAL para Snyk)
      - name: Install dependencies
        run: flutter pub get

      # 3. Instalamos Snyk CLI
      - name: Setup Snyk
        uses: snyk/actions/setup@master

      # 4. Ejecutamos Snyk apuntando al LOCKFILE
      # Esto es lo que soluciona el error de "Could not detect..."
      - name: Snyk Test
        run: snyk test --file=pubspec.lock
        env:
          SNYK_TOKEN: ${{ secrets.SNYK_TOKEN }}
        continue-on-error: true

  security-trivy:
    name: Security - Trivy
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v3

      - name: Run Trivy vulnerability scanner
        uses: aquasecurity/trivy-action@master
        with:
          scan-type: 'fs'            # 'fs' = Filesystem (busca en tus archivos)
          scan-ref: '.'              # Escanea la carpeta actual
          format: 'table'            # Te muestra una tabla bonita en el log
          exit-code: '0'             # 0 = No rompe el pipeline (aviso). Pon 1 para bloquear.
          ignore-unfixed: true       # Ignora fallos que no tienen solución todavía
          severity: 'CRITICAL,HIGH'  # Solo avisa de cosas graves